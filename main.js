class Game {
    constructor(canvas, context){
        this.canvas = canvas;
        this.context = context;
        thiss.width = this.canvas.width;
        this.height = this.canvas.height;
    }
        

    }

window.addEventListener('load', function(){
    const convas = this.document.getElementById('canvas1');

});